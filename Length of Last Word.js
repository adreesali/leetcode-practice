var lengthOfLastWord = function(s) {
    let result = s.trim().split(' ');
    return result[result.length-1].length
  };
  
  var kengthOfLastWord = function(a) {
      let result = a.trim().split(' ');
      return result[result.length-1].length
  }

//   2nd


var lengthOfLastWord = function(s) {
    let result = s.trim().split(' ');
    return result[result.length-1].length
  };
  
  var kengthOfLastWord = function(a) {
      let result = a.trim().split(' ');
      return result[result.length-1].length
  }